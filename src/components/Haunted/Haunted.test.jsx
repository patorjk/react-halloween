import React from 'react';
import { render } from '@testing-library/react';

import Haunted from './Haunted';

// TODO: TESTS WILL COME AT VERSION 1.0.0
describe('Haunted', () => {
  test('renders the Haunted component', () => {
    render(<Haunted />);
  });
});
